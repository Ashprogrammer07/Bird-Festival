# 🏗️ Deep Internationalization Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                              │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  Language Switcher: [EN] / [HI]                              │   │
│  │  (LanguageContext provides current language to all components)│   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        REACT COMPONENTS                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  Schedule    │  │  Festival    │  │   Gallery    │              │
│  │  Component   │  │  Component   │  │  Component   │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│         │                  │                  │                      │
│         └──────────────────┴──────────────────┘                      │
│                            │                                         │
│                  Uses: useLanguage() hook                            │
│                  Gets: { language, t }                               │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          API CALLS                                   │
│                                                                      │
│  Option 1 (Recommended):                                            │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  fetch(`/api/schedule?lang=${language}`)                   │    │
│  │  → Returns localized data (strings only)                   │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  Option 2 (Manual):                                                 │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  fetch('/api/schedule')                                     │    │
│  │  → Returns full bilingual data (objects with en/hi)        │    │
│  │  → Use deepLocalize(data, language) on frontend            │    │
│  └────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      EXPRESS CONTROLLERS                             │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  scheduleController.getAllSchedules(req, res)                │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  1. Extract lang from req.query.lang                   │ │   │
│  │  │  2. Fetch data from MongoDB                            │ │   │
│  │  │  3. If lang specified:                                 │ │   │
│  │  │     → toLocalizedObjects(data, lang)                   │ │   │
│  │  │  4. Return localized or full bilingual data            │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  Uses: server/utils/i18nHelper.js                                   │
│  - toLocalizedObject()                                              │
│  - toLocalizedObjects()                                             │
│  - deepLocalize()                                                   │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        MONGODB DATABASE                              │
│                                                                      │
│  Collection: schedules                                              │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  {                                                          │    │
│  │    "_id": "...",                                            │    │
│  │    "day": 1,                                                │    │
│  │    "date": "2024-02-15",                                    │    │
│  │    "title": {                                               │    │
│  │      "en": "Bird Watching",                                 │    │
│  │      "hi": "पक्षी दर्शन"                                    │    │
│  │    },                                                       │    │
│  │    "events": [                                              │    │
│  │      {                                                      │    │
│  │        "time": "09:00 AM",                                  │    │
│  │        "activity": {                                        │    │
│  │          "en": "Morning Bird Walk",                         │    │
│  │          "hi": "सुबह पक्षी भ्रमण"                           │    │
│  │        },                                                   │    │
│  │        "location": {                                        │    │
│  │          "en": "Keoladeo National Park",                    │    │
│  │          "hi": "केवलादेव राष्ट्रीय उद्यान"                  │    │
│  │        }                                                    │    │
│  │      }                                                      │    │
│  │    ]                                                        │    │
│  │  }                                                          │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  Similar structure for:                                             │
│  - festivalInfo, galleryImages, resourcePersons, ebooks, quizzes   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### Flow 1: User Views Content in Hindi

```
User clicks [HI] button
         │
         ▼
LanguageContext updates: language = 'hi'
         │
         ▼
Component re-renders (useEffect triggered)
         │
         ▼
API Call: fetch('/api/schedule?lang=hi')
         │
         ▼
Controller receives request
         │
         ▼
Extract lang = 'hi' from query
         │
         ▼
Fetch data from MongoDB (bilingual)
         │
         ▼
Apply deepLocalize(data, 'hi')
         │
         ▼
Return localized data:
{
  title: "पक्षी दर्शन",
  events: [
    { activity: "सुबह पक्षी भ्रमण", ... }
  ]
}
         │
         ▼
Component displays Hindi content
```

### Flow 2: Admin Creates Bilingual Content

```
Admin fills form:
- Title (English): "Bird Watching"
- शीर्षक (हिंदी): "पक्षी दर्शन"
         │
         ▼
Form submits data:
{
  title: {
    en: "Bird Watching",
    hi: "पक्षी दर्शन"
  }
}
         │
         ▼
POST /api/schedule
         │
         ▼
Controller validates both en & hi
         │
         ▼
Save to MongoDB (bilingual format)
         │
         ▼
Success response
         │
         ▼
Content available in both languages
```

---

## Helper Function Flow

### deepLocalize() Function

```
Input: Bilingual Object + Language
┌────────────────────────────────────┐
│  {                                 │
│    title: {                        │
│      en: "Bird Watching",          │
│      hi: "पक्षी दर्शन"             │
│    },                              │
│    events: [                       │
│      {                             │
│        activity: {                 │
│          en: "Walk",               │
│          hi: "भ्रमण"               │
│        }                           │
│      }                             │
│    ]                               │
│  }                                 │
└────────────────────────────────────┘
         │
         ▼ deepLocalize(obj, 'hi')
         │
    ┌────┴────┐
    │ Traverse│
    │ Object  │
    └────┬────┘
         │
    ┌────▼────────────────────────┐
    │ For each property:          │
    │ - If has .en & .hi          │
    │   → return obj[lang]        │
    │ - If array                  │
    │   → map & recurse           │
    │ - If object                 │
    │   → recurse                 │
    │ - Else                      │
    │   → return as-is            │
    └────┬────────────────────────┘
         │
         ▼
Output: Localized Object
┌────────────────────────────────────┐
│  {                                 │
│    title: "पक्षी दर्शन",           │
│    events: [                       │
│      {                             │
│        activity: "भ्रमण"           │
│      }                             │
│    ]                               │
│  }                                 │
└────────────────────────────────────┘
```

---

## Database Schema Evolution

### Before (Monolingual)
```javascript
{
  title: String,
  description: String,
  location: String
}
```

### After (Bilingual)
```javascript
{
  title: {
    en: String (required),
    hi: String (required)
  },
  description: {
    en: String (required),
    hi: String (required)
  },
  location: {
    en: String (required),
    hi: String (required)
  }
}
```

---

## Component Integration Pattern

```
┌─────────────────────────────────────────────────────────────┐
│  Component Lifecycle                                        │
│                                                             │
│  1. Mount                                                   │
│     ↓                                                       │
│  2. Get language from context                              │
│     const { language } = useLanguage()                     │
│     ↓                                                       │
│  3. Fetch data with language                               │
│     fetch(`/api/endpoint?lang=${language}`)                │
│     ↓                                                       │
│  4. Receive localized data                                 │
│     setData(localizedData)                                 │
│     ↓                                                       │
│  5. Render with localized content                          │
│     <h1>{data.title}</h1>                                  │
│     ↓                                                       │
│  6. Language changes (user clicks switcher)                │
│     ↓                                                       │
│  7. useEffect triggered (language dependency)              │
│     ↓                                                       │
│  8. Re-fetch with new language                             │
│     ↓                                                       │
│  9. Re-render with new localized content                   │
└─────────────────────────────────────────────────────────────┘
```

---

## File Structure

```
Bird-Festival/
├── server/
│   ├── models/
│   │   ├── Schedule.js          ✅ Updated (bilingual)
│   │   ├── FestivalInfo.js      ✅ Updated (bilingual)
│   │   ├── GalleryImage.js      ✅ Updated (bilingual)
│   │   ├── ResourcePerson.js    ✅ Updated (bilingual)
│   │   ├── Ebook.js             ✅ Updated (bilingual)
│   │   └── Quiz.js              ✅ Updated (bilingual)
│   │
│   ├── controllers/
│   │   ├── scheduleController.js     ✅ Updated (i18n support)
│   │   ├── festivalController.js     ✅ Updated (i18n support)
│   │   ├── galleryController.js      ✅ Updated (i18n support)
│   │   ├── ebookController.js        ✅ Updated (i18n support)
│   │   ├── quizController.js         ✅ Updated (i18n support)
│   │   └── resourcePersonController.js ✅ Updated (i18n support)
│   │
│   ├── utils/
│   │   └── i18nHelper.js        ✨ New (helper functions)
│   │
│   └── migrate-to-bilingual.js  ✨ New (migration script)
│
├── client/
│   └── src/
│       ├── context/
│       │   └── LanguageContext.jsx   ✅ Existing (provides language)
│       │
│       ├── utils/
│       │   └── i18nHelper.js    ✨ New (frontend helpers)
│       │
│       └── examples/
│           └── ScheduleExample.jsx   ✨ New (reference implementation)
│
├── INTERNATIONALIZATION.md      ✨ New (full documentation)
├── IMPLEMENTATION_SUMMARY.md    ✨ New (summary)
├── QUICK_START.md              ✨ New (quick guide)
└── ARCHITECTURE.md             ✨ New (this file)
```

---

## Technology Stack

```
┌─────────────────────────────────────────────────────────────┐
│  Frontend                                                   │
│  ├── React (UI Components)                                 │
│  ├── LanguageContext (State Management)                    │
│  └── i18nHelper.js (Localization Utilities)                │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  API Layer                                                  │
│  ├── Express.js (REST API)                                 │
│  ├── Query Parameter: ?lang=en|hi                          │
│  └── Controllers (Business Logic)                          │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Utilities                                                  │
│  ├── i18nHelper.js (Backend)                               │
│  │   ├── deepLocalize()                                    │
│  │   ├── toLocalizedObject()                               │
│  │   └── toLocalizedObjects()                              │
│  └── Migration Script                                      │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Database                                                   │
│  ├── MongoDB (Document Store)                              │
│  ├── Mongoose (ODM)                                        │
│  └── Bilingual Schema (en/hi objects)                      │
└─────────────────────────────────────────────────────────────┘
```

---

## Performance Considerations

### Server-Side Localization (Recommended)
```
Pros:
✅ Smaller payload (strings instead of objects)
✅ Less frontend processing
✅ Better performance
✅ Cleaner component code

Cons:
❌ Requires API call on language change
```

### Client-Side Localization
```
Pros:
✅ No API call on language change
✅ Instant language switching

Cons:
❌ Larger payload (full bilingual data)
❌ More frontend processing
❌ More complex component code
```

---

*This architecture enables a fully bilingual experience while maintaining code quality and performance.*
